import Vue from "vue"
import App from "./QA.vue"
import {
  Header,
  Button,
  Popup,
  Spinner,
  InfiniteScroll
} from "mint-ui"

Vue.component(Header.name, Header)
Vue.component(Button.name, Button)
Vue.component(Popup.name, Popup)
Vue.component(Spinner.name, Spinner)

Vue.use(InfiniteScroll)

/* eslint-disable no-new */
new Vue({
  el: "#app",
  render: h => h(App)
})
